# Discovery (TypeScript/Fastify)

Minimal resource registry: `/health`, `/resources`, `/resources/search` (port 8090).

## Dev
```
cd services/discovery
npm i --omit=dev
npm start
# or via compose
make dev
```
