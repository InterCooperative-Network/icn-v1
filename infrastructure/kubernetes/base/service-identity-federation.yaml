apiVersion: v1
kind: Service
metadata:
  name: identity-federation-service
  namespace: icn-platform
  labels:
    app: identity-federation
spec:
  selector:
    app: identity-federation
  ports:
  - name: http
    port: 80
    targetPort: 3001
  type: ClusterIP


